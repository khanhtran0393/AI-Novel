# Tasks: Automations & Headless Engine

- [x] Configure `serverExternalPackages` in `next.config.ts`.
- [x] Implement `imageModule.ts` with parallel worker dispatch.
- [x] Integrate Google Cookie rotation logic inside `imageModule.ts`.
- [x] Implement `fs.rmSync` in `finally` block for Chromium profile cleanup.
- [x] Implement `ttsModule.ts` with `Cache Storage API` integration.
- [x] Implement `folderModule.ts` to spawn `explorer.exe` via `child_process`.
