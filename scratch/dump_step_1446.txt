"    }\n  };\n\n  const handleExpandScene = async (index: number) => {\n    if (!currentChapter) return;\n    const scenes = parseScenes(streamText);\n    const sceneToExpand = scenes[index];\n    if (!sceneToExpand || !sceneToExpand.content.trim()) return;\n\n    store.setDangTai(true);\n    \n    try {\n      const res = await fetch('/api/generate', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({\n          requestType: 'EXPAND_SCENE',\n          apiKey: store.apiKey,\n          apiKeys: store.apiKeys,\n          payload: {\n            ten_tac_pham: store.ten_tac_pham,\n            chuong_hien_tai: currentChapter,\n            lorebook: store.lorebook,\n            previous_scene_content: index > 0 ? scenes[index - 1].content : '',\n            current_scene_content: sceneToExpand.content,\n            next_scene_content: index < scenes.length - 1 ? scenes[index + 1].content : ''\n          }\n        })\n      });\n\n      if (!res.ok) {\n        const err = await res.json();\n        throw new Error(err.error || 'Lỗi khi kết nối với AI (Expart).');\n      }\n\n      const data = await res.json();\n      if (data.usedApiKey) store.prioritizeApiKey(data.usedApiKey);\n      const expandedContent = (data.expanded_content || sceneToExpand.content).normalize('NFC');\n      \n      handleSceneChange(index, expandedContent);\n\n    } catch (err: any) {\n      alert(err.message);\n    } finally {\n      store.setDangTai(false);\n    }\n  };\n\n  // Chốt bước 3 (Script) và đi đến bước 4 (Commit Memory)"