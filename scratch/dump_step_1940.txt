"      {/* ====================================================================== */}\n      {/* 🎙️ TTS VOICE AUTOMATE DRAWER */}\n      {/* ====================================================================== */}\n      {activeTtsSceneIndex !== null && (() => {\n        const scenes = parseScenes(streamText);\n        const scene = scenes[activeTtsSceneIndex];\n        if (!scene) return null;\n        const cleanScript = cleanVoiceScript(scene.content);\n\n        return (\n          <div className=\"fixed inset-0 z-50 flex justify-end bg-black/60 backdrop-blur-sm animate-in fade-in duration-200\">\n            <div className=\"absolute inset-0 cursor-pointer\" onClick={() => { setActiveTtsSceneIndex(null); handleStopTTS(); }} />\n            \n            <div className=\"relative w-full max-w-2xl h-full border-l border-zinc-800 bg-zinc-950 p-6 shadow-2xl flex flex-col gap-6 overflow-y-auto animate-in slide-in-from-right duration-300\">\n              {/* Header */}\n              <div className=\"flex items-center justify-between border-b border-zinc-900 pb-4\">\n                <div className=\"flex flex-col gap-1\">\n                  <span className=\"text-[9px] font-bold uppercase tracking-widest text-sky-400\">🎙️ TRÌNH TỰ ĐỘNG HÓA GIỌNG NÓI AI (GOOGLE AI STUDIO SPEECH)</span>\n                  <h2 className=\"text-sm font-extrabold text-zinc-100 uppercase tracking-wider\">\n                    {scene.title === 'MỞ ĐẦU' || scene.title === 'KỊCH BẢN' ? scene.title : `TTS CẢNH ${activeTtsSceneIndex}`}\n                  </h2>\n                </div>\n                <button\n                  onClick={() => { setActiveTtsSceneIndex(null); handleStopTTS(); }}\n                  className=\"rounded-full border border-zinc-800 bg-zinc-900/60 p-2 text-zinc-400 hover:bg-zinc-800 hover:text-zinc-100 transition-colors cursor-pointer\"\n                >\n                  <X className=\"h-4 w-4\" />\n                </button>\n              </div>\n\n              {/* Body */}\n  
<truncated 16784 bytes>