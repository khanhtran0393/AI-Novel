"const fs = require('fs');\nconst path = require('path');\n\nfunction searchSSTWildcard() {\n  const dir = 'd:\\\\chuyen gia mac the app\\\\.next\\\\dev\\\\cache\\\\turbopack\\\\ee6e79b1';\n  const files = fs.readdirSync(dir);\n  \n  const searchTerms = ['parseScenes', 'cleanVoiceScript', 'giai_doan', 'tom_tat_cuon_chieu', 'tri_nho_ngan_han'];\n  \n  files.forEach(file => {\n    if (file.endsWith('.sst')) {\n      const fullPath = path.join(dir, file);\n      try {\n        const stat = fs.statSync(fullPath);\n        if (stat.size > 10000) {\n          const content = fs.readFileSync(fullPath, 'binary');\n          searchTerms.forEach(term => {\n            const idx = content.indexOf(term);\n            if (idx !== -1) {\n              console.log(`FOUND \"${term}\" in file: ${file}, Size: ${stat.size}, index: ${idx}`);\n            }\n          });\n        }\n      } catch (e) {}\n    }\n  });\n}\n\nsearchSSTWildcard();\n"